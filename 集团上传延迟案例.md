### 4.2 补充分析路径配置（续）

```markdown
#### 任务延期分析路径配置

| 步骤 | 动作 | 调用工具 | 判断条件 | 下一步 |
|------|------|----------|----------|--------|
| 1 | 获取任务状态 | get_task_status | - | 2 |
| 2 | 判断任务状态 | - | status=completed | 结束(非阻塞) |
| 2 | 判断任务状态 | - | status=failed | 4 |
| 2 | 判断任务状态 | - | status=waiting/pending | 3 |
| 3 | 获取上游依赖 | get_upstream_dependencies | - | 5 |
| 4 | 获取错误信息 | get_task_error_info | - | 7 |
| 5 | 遍历检查上游状态 | get_task_status | 全部completed | 6 |
| 5 | 遍历检查上游状态 | get_task_status | 存在未完成 | 递归步骤1 |
| 6 | 检查运行条件 | check_run_conditions | - | 7 |
| 7 | 核实根因详情 | verify_root_cause | - | 8 |
| 8 | 生成分析报告 | generate_report | - | 9 |
| 9 | 推送通知 | send_notification | - | 结束 |
```

#### 分析路径配置表数据示例

```sql
-- 插入任务延期分析路径配置
INSERT INTO ops_analysis_path (path_id, problem_type, step_order, step_name, tool_code, tool_params, next_step_condition) VALUES
('PATH_DELAY_001', 'task_delay', 1, '获取任务状态', 'get_task_status', '{"task_id": "${task_id}"}', NULL),
('PATH_DELAY_002', 'task_delay', 2, '判断任务状态-已完成', NULL, NULL, 'status == "completed" -> END_NOT_BLOCKER'),
('PATH_DELAY_003', 'task_delay', 2, '判断任务状态-失败', NULL, NULL, 'status == "failed" -> STEP_4'),
('PATH_DELAY_004', 'task_delay', 2, '判断任务状态-等待', NULL, NULL, 'status in ("waiting","pending") -> STEP_3'),
('PATH_DELAY_005', 'task_delay', 3, '获取上游依赖', 'get_upstream_dependencies', '{"task_id": "${task_id}"}', NULL),
('PATH_DELAY_006', 'task_delay', 4, '获取错误信息', 'get_task_error_info', '{"task_id": "${task_id}"}', NULL),
('PATH_DELAY_007', 'task_delay', 5, '遍历检查上游', 'batch_get_task_status', '{"task_ids": "${upstream_task_ids}"}', 'has_incomplete -> RECURSIVE_STEP_1'),
('PATH_DELAY_008', 'task_delay', 6, '检查运行条件', 'check_run_conditions', '{"task_id": "${task_id}"}', NULL),
('PATH_DELAY_009', 'task_delay', 7, '核实根因详情', 'verify_root_cause', '{"cause_type": "${cause_type}", "cause_detail": "${cause_detail}"}', NULL),
('PATH_DELAY_010', 'task_delay', 8, '生成分析报告', 'generate_report', '{"analysis_result": "${analysis_result}"}', NULL),
('PATH_DELAY_011', 'task_delay', 9, '推送通知', 'send_notification', '{"report": "${report}", "channels": ["dingtalk","sms","qmx"]}', NULL);
```

---

### 4.3 补充根因类型字典

```markdown
#### 根因类型分类表（ops_root_cause_type）

| 类型编码 | 类型名称 | 描述 | 典型场景 | 建议处理方式 |
|----------|----------|------|----------|--------------|
| FILE_NOT_ARRIVED | 源文件未送达 | 外部系统文件未按时送达 | 接口文件、FTP文件缺失 | 联系数据提供方 |
| FILE_FORMAT_ERROR | 文件格式错误 | 文件内容格式不符合预期 | 字段缺失、编码错误 | 联系数据提供方修正 |
| UPSTREAM_FAILED | 上游任务失败 | 依赖的上游任务执行失败 | SQL错误、数据异常 | 修复上游任务后重跑 |
| UPSTREAM_DELAYED | 上游任务延迟 | 依赖的上游任务执行超时 | 数据量大、资源不足 | 等待上游完成或优化 |
| RESOURCE_INSUFFICIENT | 资源不足 | CPU/内存/磁盘资源不足 | 大数据量处理场景 | 扩容或分批处理 |
| DATA_QUALITY_ISSUE | 数据质量问题 | 数据缺失/重复/不一致 | 主键冲突、空值过多 | 数据质量治理 |
| SQL_ERROR | SQL执行错误 | SQL语法或逻辑错误 | 语法错误、表不存在 | 修复SQL后重跑 |
| NETWORK_ISSUE | 网络问题 | 网络连接异常 | 跨集群访问失败 | 联系网络运维 |
| PERMISSION_DENIED | 权限不足 | 用户无操作权限 | 表/目录权限缺失 | 申请权限 |
| SYSTEM_ERROR | 系统错误 | 平台/组件异常 | Hive/Spark服务异常 | 联系平台运维 |
| EXTERNAL_DEPENDENCY | 外部依赖异常 | 外部系统/服务不可用 | API调用失败 | 联系外部系统 |
| SCHEDULING_CONFLICT | 调度冲突 | 任务调度配置冲突 | 时间窗口冲突、并发限制 | 调整调度配置 |
```

---

### 4.4 补充AI辅助分析提示词

```markdown
#### 任务延期根因分析AI提示词模板

你是一位资深的数据运维专家，请根据以下信息分析任务延期的根本原因。

## 告警信息
- 任务名称：{task_name}
- 任务ID：{task_id}
- 预计完成时间：{expected_time}
- 实际状态：{current_status}
- 延期时长：{delay_minutes}分钟

## 依赖链路追溯结果
{dependency_trace_result}

## 任务执行日志（最近）
{recent_logs}

## 历史执行情况
{historical_execution}

## 请分析以下内容：

1. **根因确认**：确认追溯结果中的根因是否准确，是否有其他可能的原因
2. **影响评估**：评估该问题对下游任务和业务的影响范围
3. **处理建议**：给出具体可执行的处理步骤
4. **预防措施**：建议如何预防类似问题再次发生

## 输出格式
```json
{
  "root_cause_confirmed": true/false,
  "root_cause_analysis": "根因分析详情",
  "alternative_causes": ["其他可能原因"],
  "impact_assessment": {
    "affected_tasks": ["受影响任务列表"],
    "business_impact": "业务影响说明",
    "severity": "高/中/低"
  },
  "immediate_actions": [
    {"step": 1, "action": "具体操作", "executor": "执行人/角色"}
  ],
  "prevention_suggestions": ["预防建议"]
}
```
```

---

### 4.5 补充接口定义

```markdown
#### 3.3.3.7 依赖链路追溯接口

**接口定义：**

| 属性 | 说明 |
|------|------|
| 接口名称 | 任务依赖链路追溯接口 |
| 接口方式 | RESTful API |
| 接口URL | /ops/dependency/trace |
| 调用方式 | POST |

**输入参数：**
```json
{
  "task_id": "JT_PROD_INST_UPLOAD",
  "trace_direction": "upstream",
  "max_depth": 10,
  "include_completed": false
}
```

**输出参数：**
```json
{
  "resultCode": 0,
  "resultMsg": "成功",
  "resultData": {
    "trace_id": "TRACE_20260420_001",
    "target_task": {
      "task_id": "JT_PROD_INST_UPLOAD",
      "task_name": "集团上传产品实例表",
      "status": "waiting"
    },
    "trace_depth": 2,
    "trace_path": [
      {
        "depth": 0,
        "task_id": "JT_PROD_INST_UPLOAD",
        "status": "waiting",
        "upstream_tasks": ["TASK_A", "TASK_B", "TASK_C"],
        "blocking_tasks": ["TASK_C"]
      },
      {
        "depth": 1,
        "task_id": "TASK_C",
        "status": "waiting",
        "upstream_tasks": ["TASK_C1", "TASK_C2"],
        "blocking_tasks": ["TASK_C2"]
      },
      {
        "depth": 2,
        "task_id": "TASK_C2",
        "status": "failed",
        "error_type": "FILE_NOT_ARRIVED",
        "error_message": "源文件不存在: /data/interface/prod_inst_20260420.dat"
      }
    ],
    "root_cause": {
      "task_id": "TASK_C2",
      "task_name": "源系统接口采集",
      "cause_type": "FILE_NOT_ARRIVED",
      "cause_detail": "文件 /data/interface/prod_inst_20260420.dat 未送达",
      "cause_time": "2026-04-20 05:30:00"
    },
    "impact_chain": ["TASK_C2", "TASK_C", "JT_PROD_INST_UPLOAD"],
    "trace_duration_ms": 11000
  }
}
```

#### 3.3.3.8 文件状态检查接口

**接口定义：**

| 属性 | 说明 |
|------|------|
| 接口名称 | 文件状态检查接口 |
| 接口方式 | RESTful API |
| 接口URL | /ops/file/check |
| 调用方式 | POST |

**输入参数：**
```json
{
  "file_path": "/data/interface/prod_inst_20260420.dat",
  "check_history": true,
  "history_days": 7
}
```

**输出参数：**
```json
{
  "resultCode": 0,
  "resultMsg": "成功",
  "resultData": {
    "file_path": "/data/interface/prod_inst_20260420.dat",
    "file_exists": false,
    "check_time": "2026-04-20 08:30:31",
    "expected_time": "2026-04-20 05:30:00",
    "delay_minutes": 180,
    "source_info": {
      "source_system": "集团CRM系统",
      "source_contact": "集团数据组-张三",
      "contact_phone": "13912345678"
    },
    "history": {
      "check_days": 7,
      "success_count": 7,
      "success_rate": 1.0,
      "avg_arrival_time": "05:15",
      "records": [
        {"date": "2026-04-19", "arrived": true, "arrival_time": "05:12"},
        {"date": "2026-04-18", "arrived": true, "arrival_time": "05:18"},
        {"date": "2026-04-17", "arrived": true, "arrival_time": "05:10"}
      ]
    }
  }
}
```

---

### 4.6 补充案例学习机制

```markdown
#### 3.3.4.4 案例自动沉淀机制

当根因分析完成并经人工确认后，系统自动将本次分析沉淀为案例：

**沉淀触发条件：**
1. 根因分析完成
2. 问题得到解决（相关任务恢复执行）
3. 人工确认分析结果准确（可选）

**案例沉淀内容：**
```json
{
  "case_id": "CASE_20260420_001",
  "case_type": "task_delay",
  "create_time": "2026-04-20 10:30:00",

  "problem_feature": {
    "task_pattern": "集团上传*",
    "error_pattern": "源文件不存在",
    "keywords": ["文件未送达", "接口采集", "集团上传"]
  },

  "root_cause": {
    "type": "FILE_NOT_ARRIVED",
    "description": "源系统文件未按时送达",
    "trace_depth": 2
  },

  "solution": {
    "steps": [
      "联系数据提供方确认文件状态",
      "确认文件送达后触发任务重跑",
      "验证下游任务恢复正常"
    ],
    "resolution_time_minutes": 120,
    "effectiveness": "有效"
  },

  "prevention": {
    "suggestions": [
      "增加文件送达预警监控",
      "设置文件送达超时自动告警"
    ],
    "implemented": false
  },

  "usage_stats": {
    "match_count": 0,
    "success_count": 0,
    "avg_resolution_time": null
  }
}
```

**案例匹配算法：**
```python
def match_case(current_problem):
    """
    案例匹配算法
    """
    candidates = query_cases_by_type(current_problem['type'])
  
    scored_cases = []
    for case in candidates:
        score = 0
      
        # 1. 任务名称模式匹配 (权重30%)
        if match_pattern(current_problem['task_name'], case['task_pattern']):
            score += 0.3
      
        # 2. 错误信息相似度 (权重40%)
        error_similarity = calculate_text_similarity(
            current_problem['error_message'],
            case['error_pattern']
        )
        score += 0.4 * error_similarity
      
        # 3. 关键词匹配 (权重20%)
        keyword_match_rate = len(
            set(current_problem['keywords']) & set(case['keywords'])
        ) / len(case['keywords'])
        score += 0.2 * keyword_match_rate
      
        # 4. 历史有效性 (权重10%)
        if case['usage_stats']['match_count'] > 0:
            effectiveness = case['usage_stats']['success_count'] / case['usage_stats']['match_count']
            score += 0.1 * effectiveness
      
        scored_cases.append((case, score))
  
    # 按分数排序
    scored_cases.sort(key=lambda x: x[1], reverse=True)
  
    # 返回最佳匹配（阈值0.85）
    if scored_cases and scored_cases[0][1] >= 0.85:
        return scored_cases[0]
  
    return None
```
```

---

## 五、完整案例文档（可作为附录）

建议将以上内容整理为设计文档的附录，标题为：

```markdown
## 附录A：根因分析典型案例

### A.1 案例概述

| 案例编号 | 案例名称 | 根因类型 | 分析耗时 |
|----------|----------|----------|----------|
| CASE_001 | 集团上传产品实例表延期 | 源文件未送达 | 30秒 |

### A.2 案例详情

#### A.2.1 告警信息
（内容如上）

#### A.2.2 人工排查路径对比
（内容如上）

#### A.2.3 数字员工分析流程
（内容如上）

#### A.2.4 分析结果输出
（内容如上）

### A.3 效果对比

| 对比维度 | 人工处理 | 数字员工 | 提升效果 |
|----------|----------|----------|----------|
| 分析耗时 | 45分钟 | 3分钟 | 提升93% |
| 涉及系统 | 3个 | 自动化 | - |
| 人工介入 | 全程 | 仅确认 | 减少90% |
| 结果输出 | 口头/文字 | 结构化报告 | 标准化 |
```

---

## 六、总结

通过增加这个完整案例，可以：

1. **具象化展示**：让评审人员清晰理解根因分析的执行过程
2. **验证可行性**：证明设计方案能够解决实际业务问题
3. **指导开发**：为开发人员提供详细的实现参考
4. **便于测试**：可作为测试用例的参考场景

**建议将此案例添加到设计文档的以下位置：**
- 3.3.2节后作为"3.3.2.5 典型案例"
- 或作为"附录A：根因分析典型案例"
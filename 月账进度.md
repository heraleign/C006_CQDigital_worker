# 月账进度管理产品设计方案

## 1. 产品概述
本产品用于管理重庆电信月度账务的 134 项作业任务，支持任务配置、进度监控、甘特图展示与钉钉简报推送。
**核心场景**：每月账期（如 `202601`）自动或手动生成任务清单，实时跟踪任务执行状态，通过 Web 甘特图概览进度，并在钉钉群自动发送进度简报。

## 2. 功能模块

| 模块 | 说明 |
|------|------|
| **任务配置** | 支持逐项添加、编辑任务，或通过 Excel 批量导入 |
| **账期管理** | 创建新账期（从模板复制或全新配置），按月查询 |
| **泳道里程碑甘特图** | 按“前置/用户/实收/应收/集团”五个泳道展示任务时间轴与状态 |
| **进度简报** | 自动生成文字版进度，推送至钉钉群 |
| **状态更新** | 人工或系统自动更新任务状态（未开始/进行中/已完成/异常） |
| **依赖管理** | 定义任务间的前置关系，展示依赖箭头 |

## 3. 数据库设计

### 3.1 表结构（MySQL 示例）

```sql
-- 账期主表
CREATE TABLE billing_cycle (
    cycle_id VARCHAR(6) PRIMARY KEY,   -- 如 '202601'
    cycle_name VARCHAR(20),
    start_date DATE,
    end_date DATE,
    status ENUM('active','closed') DEFAULT 'active'
);

-- 任务配置表（每个账期独立记录）
CREATE TABLE monthly_task (
    task_id INT AUTO_INCREMENT PRIMARY KEY,
    cycle_id VARCHAR(6) NOT NULL,
    task_code VARCHAR(50) NOT NULL,            -- 如 'PRE_END_001'
    task_name VARCHAR(200),
    work_type ENUM('前置作业','用户作业','实收作业','应收作业') NOT NULL,
    planned_start DATETIME NOT NULL,           -- 计划开始时间
    planned_end DATETIME,                      -- 计划结束时间（若为点任务则与开始相同）
    duration_minutes INT DEFAULT 0,           -- 预计耗时（分钟），便于展示宽度
    dependency_codes VARCHAR(500),            -- 前置任务编码，逗号分隔，如 'PRE_END_001,PRE_END_002'
    assignee VARCHAR(50),                     -- 负责人
    status ENUM('未开始','进行中','已完成','异常') DEFAULT '未开始',
    actual_start DATETIME,
    actual_end DATETIME,
    remark TEXT,
    FOREIGN KEY (cycle_id) REFERENCES billing_cycle(cycle_id)
);

-- 可选：任务执行日志表
CREATE TABLE task_log (
    log_id INT AUTO_INCREMENT PRIMARY KEY,
    task_id INT,
    old_status VARCHAR(20),
    new_status VARCHAR(20),
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    operator VARCHAR(50)
);
```

### 3.2 样例数据（202601 账期，部分任务）

```sql
INSERT INTO billing_cycle VALUES ('202601','2026年1月账期','2026-01-31','2026-02-03','active');

INSERT INTO monthly_task (cycle_id, task_code, task_name, work_type, planned_start, planned_end, dependency_codes, status)
VALUES
('202601','PRE_END_001','截图 otter 同步情况','前置作业','2026-01-31 23:30:00','2026-01-31 23:35:00',NULL,'已完成'),
('202601','PRE_END_002','发布”账期准备开始”消息','前置作业','2026-01-31 23:55:00','2026-01-31 23:55:00','PRE_END_001','已完成'),
('202601','PRE_D1_001','启动计费1号批次接口层采集','前置作业','2026-02-01 00:20:00','2026-02-01 00:25:00','PRE_END_002','已完成'),
('202601','PRE_D1_022','启动余额账本整合层数据','前置作业','2026-02-01 18:40:00','2026-02-01 18:50:00','PRE_D1_020,PRE_D1_021','进行中'),
('202601','USER_D1_001','月认领基础表波动量审核','用户作业','2026-02-01 10:00:00','2026-02-01 10:30:00',NULL,'未开始'),
('202601','INCOME_D1_001','调度前置任务点击立即执行','实收作业','2026-02-01 10:30:00','2026-02-01 10:35:00',NULL,'未开始'),
('202601','REC_D1_001','D153的1号批次处理','应收作业','2026-02-01 14:00:00','2026-02-01 14:30:00',NULL,'未开始');
-- 其余的 127 条按同样规则插入...
```

## 4. 前台展现设计（Web 页面）

### 4.1 页面布局
- **顶部筛选栏**：账期选择器（下拉，如 `202601`）、刷新按钮、导出按钮、简报推送按钮。
- **主区域**：泳道式里程碑甘特图，使用 **ECharts 甘特图（自定义系列）** 或 **AntV G2/G6** 实现。
  - **纵向泳道**：
    - 五个区块：前置作业、用户作业、实收作业、应收作业、集团作业
    - 每个区块内按任务 `task_code` 排序，密集排列
  - **横向时间轴**：
    - 起始时间：账期最早 `planned_start`（如 1月31日 23:00）
    - 结束时间：最晚 `planned_end`（如 2月3日 21:30）
    - 刻度：自适应，默认每 2 小时一个刻度，可缩放
  - **任务条**：
    - 位置：根据 `planned_start` 和 `planned_end` 绘制条块，宽度代表 `duration_minutes`
    - 颜色：未开始=灰，进行中=蓝，已完成=绿，异常=红
    - 悬浮提示：显示任务编码、名称、状态、计划时间、负责人、依赖
  - **依赖箭头**：解析 `dependency_codes`，绘制从前置任务末端到当前任务始端的箭头（G6 图引擎支持）
  - **当前时刻红线**：用 `DATEDIFF` 实时标记
  - **进度汇总**：每个泳道头顶显示完成百分比

### 4.2 示例截图描述（概念）
```
[202601 ▾] [刷新] [推送简报]

┌──────────────────────────────────────────────────────────────┐
│ 前置作业 ██████████████░░░░░░░░░░░░░░░░░░ 完成 68%           │
│  ├ PRE_END_001 ■■■■■ (已完成)                                │
│  ├ PRE_D1_022 ░░░█ (进行中)                                  │
│  ...                                                         │
│ 用户作业 ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 0%                  │
│  ├ USER_D1_001 ░░░░                                          │
│  ...                                                         │
│ 实收作业 ...                                                 │
│ 应收作业 ...                                                 │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 交互细节
- 点击任务条可编辑状态（人工确认）。
- 缩放与平移：支持拖拽时间轴查看细节。
- 异常告警：异常任务在右侧面板闪烁提醒。

## 5. 后台查询接口设计（RESTful）

### 5.1 获取甘特图数据
**接口**：`GET /api/tasks/gantt?cycle_id=202601`
**响应**：
```json
{
  "cycle": "202601",
  "base_time": "2026-01-31T23:00:00",
  "tasks": [
    {
      "task_code": "PRE_END_001",
      "task_name": "截图 otter 同步情况",
      "work_type": "前置作业",
      "planned_start": "2026-01-31T23:30:00",
      "planned_end": "2026-01-31T23:35:00",
      "status": "已完成",
      "dependencies": []
    },
    ...
  ]
}
```
前端直接使用此数据构造甘特图表。

### 5.2 更新任务状态
**接口**：`PUT /api/tasks/{task_id}/status`
**请求体**：`{ "status": "已完成", "actual_end": "2026-01-31T23:35:00" }`

### 5.3 生成简报 & 钉钉推送
**接口**：`POST /api/cycle/{cycle_id}/brief`
**逻辑**：
- 统计各作业完成率
- 汇总进行中与异常任务
- 生成文本：
```
【月账进度 202601 01-31 23:35】
✅前置作业：已完成 2/61（3%）
🔄进行中：PRE_D1_001（采集拍照）
⏳待执行：用户作业、实收作业等
⚠️异常：无
当前进度正常。
```
- 调用钉钉 Webhook 发送消息。

### 5.4 Excel 导入
**接口**：`POST /api/cycle/{cycle_id}/import` （multipart 文件上传）
Excel 模板列：`task_code, task_name, work_type, planned_start, planned_end, dependency_codes, assignee`
后端解析后批量插入 `monthly_task`，自动生成 `task_id`。

### 5.5 新增任务（逐项）
**接口**：`POST /api/tasks`
请求体同 monthly_task 字段（除 task_id）

## 6. 钉钉群简报设计
- **触发方式**：手动推送 + 定时推送（如每小时自动）
- **消息格式**：Markdown，@相关人员
- **定时规则**：仅在 key milestones 前后发送，如 1号凌晨、1号10点、14点、18点等。
- **异常即时推送**：当任务状态变更为“异常”时，实时发告警消息。

## 7. 关键设计说明
- **泳道甘特图**：选择 G2Plot Gantt 或 ECharts 自定义系列，四条泳道用不同背景区分，任务条用 CSS 渐变表示进度。
- **依赖可视化**：对于复杂依赖，采用 G6 图引擎绘制迷你网络，但更简单的是直接解析依赖，用连线从任务末点画至目标始点。
- **账期隔离**：通过 `cycle_id` 完全隔离数据，历史账期可查询、不可修改。
- **Excel 导入/导出**：Apache POI 处理，模板固定前7列，支持批量导入任意数量的任务。

## 8. 部署与扩展
- 前端：React/Vue + AntV/G2
- 后端：Spring Boot / Node.js
- 数据库：MySQL
- 钉钉集成：Webhook 机器人
- 可扩展：未来增加自动执行触发器、数字员工对接等。

以上设计完整覆盖需求，以 134 条任务为样本，可快速落地实现月账进度管理。
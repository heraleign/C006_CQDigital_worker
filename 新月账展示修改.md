# 月账操作线上化管理系统 - 详细设计文档

## 1. 系统概述

将月账操作Excel线上化，实现4级管理：**作业阶段 → 里程碑 → 作业计划 → 任务列表**，支持文字解析自动拆解任务。

---

## 2. 数据模型设计

### 2.1 四级层次关系

```
作业阶段 (t_stage)
  └── 里程碑 (t_milestone)
        └── 作业计划 (t_work_plan)
              └── 任务列表 (t_task)
```

### 2.2 表结构设计

#### t_stage（作业阶段表）
```sql
CREATE TABLE t_stage (id          INTEGER PRIMARY KEY AUTOINCREMENT,
    name        VARCHAR(50) NOT NULL UNIQUE,  -- 如：用户作业、前置作业
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### t_milestone（里程碑表）
```sql
CREATE TABLE t_milestone (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    stage_id    INTEGER NOT NULL REFERENCES t_stage(id),
    name        VARCHAR(100) NOT NULL,  -- 如：前置作业-1号任务
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### t_work_plan（作业计划表）
```sql
CREATE TABLE t_work_plan (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    milestone_id    INTEGER NOT NULL REFERENCES t_milestone(id),
    seq_no          INTEGER NOT NULL,               -- 作业顺序/序号
    name            VARCHAR(200) NOT NULL,           -- 作业计划名称
    time_point      VARCHAR(100),                    -- 时间点，如"1日10:00"
    exec_template   TEXT,                            -- 作业执行情况模板（原始文本）
    task_mode       VARCHAR(20) DEFAULT '人工',      -- 人工/数字员工
    is_system_task  BOOLEAN DEFAULT FALSE,           -- 是否系统任务
    sort_order      INTEGER NOT NULL DEFAULT 0,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### t_task（任务列表表）
```sql
CREATE TABLE t_task (
    idINTEGER PRIMARY KEY AUTOINCREMENT,
    work_plan_id  INTEGER NOT NULL REFERENCES t_work_plan(id),
    task_type     VARCHAR(20) NOT NULL,   -- TDP_TASK / PUBLISH_MSG / MANUAL_OP / SQL_SCRIPT
    content       TEXT NOT NULL,          -- 任务内容（TDP任务名 或 发布消息文本 或 操作描述）
    raw_text      TEXT,                   -- 原始解析来源文本片段
    sort_order    INTEGER NOT NULL DEFAULT 0,
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 2.3 任务类型枚举说明

| task_type | 含义 | 示例 |
|-----------|------|------|
| `TDP_TASK` | TDP调度任务 | `[TDP][统一处理][序号2251][月]INFBSN一号批次_1` |
| `PUBLISH_MSG` | 发布消息 | `前置作业3：计费1号批次接口层采集任务已启动` |
| `MANUAL_OP` | 人工操作步骤 | `截图计费1号批次接口层采集任务启动情况` |
| `SQL_SCRIPT` | SQL脚本 | `select count(*) from ...` |

---

## 3. 文本解析规则

### 3.1 解析逻辑

`exec_template` 字段原始文本按以下规则拆解为 `t_task` 记录：

```python
# 解析规则优先级
PARSE_RULES = [
    # TDP任务：匹配 [TDP]...格式
    {
        "type": "TDP_TASK",
        "pattern": r'\[TDP\][^\n、）]+',
    },
    # 发布消息：匹配 发布'...' 或 发布"..."格式
    {
        "type": "PUBLISH_MSG", 
        "pattern": r"发布[''\"「]([^''\"」\n]+)[''\"」]",
    },
    # SQL脚本：匹配以select/insert/create/drop/update开头的多行文本
    {
        "type": "SQL_SCRIPT",
        "pattern": r'(?i)(select|insert|create|drop|update|delete)\s+.+?(?=\n\n|\Z)',
        "flags": re.DOTALL
    },
    # 数字编号步骤（1. 2. 3.）作为人工操作
    {
        "type": "MANUAL_OP",
        "pattern": r'^\d+[\.、]\s*(.+?)(?=^\d+[\.、]|\Z)',
        "flags": re.MULTILINE | re.DOTALL
    }
]
```

### 3.2 解析示例

**输入 exec_template：**
```
1.截图计费1号批次接口层采集任务启动情况（
[TDP][统一处理][序号2251][月]INFBSN一号批次_1、
[TDP][统一处理][序号2252][月]INFBSN一号批次_2、
[TDP][统一处理][序号2253][月]INFBSN一号批次_3
）
2.发布'前置作业3：计费1号批次接口层采集任务已启动'
```

**解析输出 t_task 记录：**

| sort_order | task_type | content |
|-----------|-----------|---------|
| 1 | `MANUAL_OP` | 截图计费1号批次接口层采集任务启动情况 |
| 2 | `TDP_TASK` | [TDP][统一处理][序号2251][月]INFBSN一号批次_1 |
| 3 | `TDP_TASK` | [TDP][统一处理][序号2252][月]INFBSN一号批次_2 |
| 4 | `TDP_TASK` | [TDP][统一处理][序号2253][月]INFBSN一号批次_3 |
| 5 | `PUBLISH_MSG` | 前置作业3：计费1号批次接口层采集任务已启动 |

---

## 4. 项目结构

```
usage-hub-app/
├── backend/
│   ├── main.py
│   ├── database.py
│   ├── models.py
│   ├── schemas.py
│   ├── routers/
│   │   ├── stages.py
│   │   ├── milestones.py
│   │   ├── work_plans.py
│   │   └── tasks.py
│   ├── services/
│   │   └── task_parser.py       # 文本解析服务
│   ├── init_data.py             # 初始化Excel数据
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── StagePage.tsx         # 作业阶段列表（第1级）
│   │   │   ├── MilestonePage.tsx     # 里程碑列表（第2级）
│   │   │   ├── WorkPlanPage.tsx      # 作业计划列表（第3级）
│   │   │   └── TaskPage.tsx          # 任务列表（第4级）
│   │   ├── components/
│   │   │   ├── TemplateParseModal.tsx  # 模板解析弹窗
│   │   │   └── BreadcrumbNav.tsx       # 层级导航
│   │   ├── api/
│   │   │   └── index.ts
│   │   └── App.tsx
│   └── package.json
└── README.md
```

---

## 5. 后端 API 设计

### 5.1 基础接口

```
# 作业阶段
GET    /api/stages# 获取所有作业阶段
POST   /api/stages                    # 新增作业阶段
PUT    /api/stages/{id}               # 修改作业阶段
DELETE /api/stages/{id}               # 删除作业阶段

# 里程碑
GET    /api/stages/{stage_id}/milestones          # 获取某阶段的里程碑列表
POST   /api/milestones                            # 新增里程碑
PUT    /api/milestones/{id}                       # 修改里程碑
DELETE /api/milestones/{id}                       # 删除里程碑

# 作业计划
GET    /api/milestones/{milestone_id}/work-plans  # 获取某里程碑的作业计划列表
POST   /api/work-plans                            # 新增作业计划
PUT    /api/work-plans/{id}                       # 修改作业计划
DELETE /api/work-plans/{id}                       # 删除作业计划

# 任务列表
GET    /api/work-plans/{work_plan_id}/tasks       # 获取某作业计划的任务列表
POST   /api/tasks                                 # 新增任务
PUT    /api/tasks/{id}                            # 修改任务
DELETE /api/tasks/{id}                            # 删除任务

# 文本解析
POST   /api/parse-template            # 解析执行模板文本，返回任务列表（预览，不入库）
```

### 5.2 parse-template 接口

**Request:**
```json
{
  "template": "1.截图计费1号批次...\n[TDP][统一处理][序号2251]..."
}
```

**Response:**
```json
{
  "tasks": [
    {"sort_order": 1, "task_type": "MANUAL_OP", "content": "截图计费1号批次接口层采集任务启动情况"},
    {"sort_order": 2, "task_type": "TDP_TASK",  "content": "[TDP][统一处理][序号2251][月]INFBSN一号批次_1"},
    {"sort_order": 3, "task_type": "PUBLISH_MSG","content": "前置作业3：计费1号批次接口层采集任务已启动"}
  ]
}
```

---

## 6. 前端页面设计

### 6.1 页面结构与下钻路由

```
/stages                          → 作业阶段列表
/stages/:stageId/milestones      → 里程碑列表
/milestones/:milestoneId/plans   → 作业计划列表
/plans/:planId/tasks             → 任务列表
```

### 6.2 各页面核心元素

**StagePage（作业阶段）**
- Table列：名称、里程碑数、操作（编辑/删除/下钻）
- 点击名称或"查看"→ 跳转里程碑页

**MilestonePage（里程碑）**
- 面包屑：作业阶段 > 当前里程碑
- Table列：名称、作业计划数、操作

**WorkPlanPage（作业计划）**
- 面包屑：作业阶段 > 里程碑 > 作业计划
- Table列：序号、计划名称、时间点、任务方式、是否系统任务、操作
- 新增/编辑弹窗含：**执行模板输入框 + "解析任务"按钮**（调用 parse-template 接口预览）

**TaskPage（任务列表）**
- 面包屑：作业阶段 > 里程碑 > 作业计划 > 任务列表
- Table列：排序、任务类型（Tag色区分）、内容
- 任务类型 Tag 颜色：
  - `TDP_TASK` → 蓝色
  - `PUBLISH_MSG` → 绿色
  - `MANUAL_OP` → 橙色
  - `SQL_SCRIPT` → 紫色

---

## 7. 初始化数据

### 7.1 作业阶段（5条）

```python
stages = [
    {"id": 1, "name": "用户作业", "sort_order": 1},
    {"id": 2, "name": "前置作业", "sort_order": 2},
    {"id": 3, "name": "实收作业", "sort_order": 3},
    {"id": 4, "name": "应收作业", "sort_order": 4},
    {"id": 5, "name": "集团作业", "sort_order": 5},
]
```

### 7.2 里程碑（19条）

```python
milestones = [
    # 用户作业
    {"stage_id": 1, "name": "用户作业-1号任务", "sort_order": 1},
    {"stage_id": 1, "name": "用户作业-2号任务", "sort_order": 2},
    {"stage_id": 1, "name": "用户作业-3号任务", "sort_order": 3},
    # 前置作业
    {"stage_id": 2, "name": "前置作业-月底任务", "sort_order": 1},
    {"stage_id": 2, "name": "前置作业-1号任务",  "sort_order": 2},
    {"stage_id": 2, "name": "前置作业-2号任务",  "sort_order": 3},
    {"stage_id": 2, "name": "前置作业-3号任务",  "sort_order": 4},
    # 实收作业
    {"stage_id": 3, "name": "实收作业-1号任务",  "sort_order": 1},
    {"stage_id": 3, "name": "实收作业-2号任务",  "sort_order": 2},
    {"stage_id": 3, "name": "实收作业-3号任务",  "sort_order": 3},
    {"stage_id": 3, "name": "实收作业-4号任务",  "sort_order": 4},
    {"stage_id": 3, "name": "实收作业-5号任务",  "sort_order": 5},
    {"stage_id": 3, "name": "实收作业-7号任务",  "sort_order": 6},
    {"stage_id": 3, "name": "实收作业-15号任务", "sort_order": 7},
    {"stage_id": 3, "name": "实收作业-25号任务", "sort_order": 8},
    {"stage_id": 3, "name": "实收作业-月底任务", "sort_order": 9},
    # 应收作业
    {"stage_id": 4, "name": "应收作业-月底任务", "sort_order": 1},
    {"stage_id": 4, "name": "应收作业-1号任务",  "sort_order": 2},
    {"stage_id": 4, "name": "应收作业-2号任务",  "sort_order": 3},
    # 集团作业
    {"stage_id": 5, "name": "集团作业-1号任务",  "sort_order": 1},
    {"stage_id": 5, "name": "集团作业-2号任务",  "sort_order": 2},
    {"stage_id": 5, "name": "集团作业-3号任务",  "sort_order": 3},
]
```

---

## 8. 关键实现要点

### 8.1 task_parser.py 核心逻辑

```python
import re
from typing import List, Dict

def parse_template(template: str) -> List[Dict]:
    tasks = []
    order = 1

    # 1. 提取TDP任务
    for m in re.finditer(r'\[TDP\][^\n、）,，]+', template):
        tasks.append({"sort_order": order, "task_type": "TDP_TASK", 
                       "content": m.group().strip(), "raw_text": m.group()})
        order += 1

    # 2. 提取发布消息
    for m in re.finditer(r"发布['\u2018\u2019\"「『]([^'\u2018\u2019\"」』\n]+)", template):
        tasks.append({"sort_order": order, "task_type": "PUBLISH_MSG",
                       "content": m.group(1).strip(), "raw_text": m.group()})
        order += 1

    # 3. 提取SQL脚本块
    for m in re.finditer(r'(?i)(select|insert|create|drop)\s[\s\S]+?(?=\n\n|\n\d+\.|\Z)', template):
        tasks.append({"sort_order": order, "task_type": "SQL_SCRIPT",
                       "content": m.group().strip(), "raw_text": m.group()})
        order += 1

    # 4. 按原始顺序重排（基于在模板中的位置）
    tasks.sort(key=lambda t: template.find(t["raw_text"]))
    for i, t in enumerate(tasks):
        t["sort_order"] = i + 1

    return tasks
```

### 8.2 依赖清单（requirements.txt）

```
fastapi==0.111.0
uvicorn==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1
```

### 8.3 启动命令

```bash
# 后端
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python init_data.py       # 初始化数据
uvicorn main:app --reload --port 8000

# 前端
cd frontend
npm install
npm start
```

---

## 9. 数据流说明

```
Excel录入流程：
用户填写exec_template → 点击"解析任务" → 
调用 POST /api/parse-template → 预览任务列表 →
确认后保存作业计划（自动写入t_task）

下钻查看流程：
作业阶段列表 → 点击行 → 里程碑列表（带stage_id筛选）→ 
点击行 → 作业计划列表（带milestone_id筛选）→ 
点击行 → 任务列表（带work_plan_id筛选）
```

---

此设计文档可直接交给 Claude Code 开发。核心复杂点集中在 `task_parser.py` 的解析逻辑，建议优先实现并用真实Excel数据验证解析准确率后再推进前端开发。